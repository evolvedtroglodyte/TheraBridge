# ==============================================================================
# Audio Transcription Pipeline - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual keys
# The .env file is gitignored and will NOT be committed to version control
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED API KEYS
# ------------------------------------------------------------------------------

# OpenAI API Key (REQUIRED for Whisper API transcription)
# Used by: pipeline.py for audio-to-text transcription
# Get from: https://platform.openai.com/api-keys
# Cost: Pay-per-use based on audio duration
OPENAI_API_KEY=sk-your-key-here

# HuggingFace Token (REQUIRED for speaker diarization)
# Used by: pipeline.py and pipeline_gpu.py for pyannote speaker identification
# Setup instructions:
#   1. Create account at https://huggingface.co
#   2. Accept model terms at https://huggingface.co/pyannote/speaker-diarization-3.1
#   3. Generate token at https://hf.co/settings/tokens
HF_TOKEN=hf_your-token-here

# ------------------------------------------------------------------------------
# OPTIONAL API KEYS
# ------------------------------------------------------------------------------

# Anthropic API Key (OPTIONAL - for future Claude integration)
# Used by: Not currently used in pipeline, reserved for future AI features
# Get from: https://console.anthropic.com/settings/keys
# Leave blank if not using Claude features
ANTHROPIC_API_KEY=

# Vast.ai API Key (OPTIONAL - for GPU instance provisioning)
# Used by: Not currently used in pipeline, for automated GPU instance management
# Get from: https://cloud.vast.ai/account/ (Account → API Key)
# Leave blank if manually provisioning Vast.ai instances
VAST_API_KEY=

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION (OPTIONAL)
# ------------------------------------------------------------------------------

# PostgreSQL Database URL (OPTIONAL - for backend integration)
# Used by: Not currently used in pipeline, reserved for session data storage
# Format: postgresql://username:password@host:port/database?sslmode=require
# Example: postgresql://user:pass@localhost:5432/therapybridge?sslmode=require
# Leave blank if pipeline is running standalone
DATABASE_URL=

# Neon Project ID (OPTIONAL - if using Neon PostgreSQL)
# Used by: Backend service, not used by pipeline directly
# Get from: Neon console > Project Settings > General
# Format: ep-xxxxxxxx-xxxx-xxxx (example: ep-withered-frost-ahsas8wd)
# Leave blank if not using Neon or if using standalone pipeline
NEON_PROJECT_ID=
