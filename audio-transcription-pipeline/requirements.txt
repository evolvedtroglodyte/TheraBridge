# Audio Transcription Pipeline - Core Dependencies
# Preprocessing + Whisper API only

# Audio processing (Python 3.13+ compatible)
# Option 1: Use pydub with audioop-lts for Python 3.13+ compatibility
pydub==0.25.1
audioop-lts>=0.2.1

# GPU audio resampling (used by gpu_audio_ops.py for high-quality sinc interpolation)
julius>=0.2.7

# OpenAI Whisper API
openai>=1.59.5

# Retry logic with exponential backoff for API resilience
tenacity>=8.2.0

# Environment management
python-dotenv>=1.0.0

# Note: ffmpeg is required for pydub audio processing
# Install via: brew install ffmpeg (macOS) or apt-get install ffmpeg (Ubuntu)
