# Server Dependencies for GPU Pipeline Web UI
# Install with: pip install -r requirements-server.txt
# Note: This DOES NOT include pipeline dependencies (use ../requirements.txt)

# FastAPI web framework
fastapi>=0.115.0

# ASGI server for production
uvicorn[standard]>=0.32.0

# File uploads and request handling
python-multipart>=0.0.9

# Pydantic for data validation (included with FastAPI but explicit for clarity)
pydantic>=2.10.0

# Optional: For better performance in production
# aiofiles>=24.0.0  # Async file operations
# gunicorn>=23.0.0  # Production WSGI server
